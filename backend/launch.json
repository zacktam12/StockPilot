{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch localhost",
      "type": "firefox",
      "request": "launch",
      "reAttach": true,
      "url": "http://localhost:5000", // or whatever your Vite dev server runs on
      "webRoot": "${workspaceFolder}"
    }
  ]
}
